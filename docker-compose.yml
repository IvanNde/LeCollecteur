version: '3.8'
services:
  collecteur:
    build: .
    container_name: collecteur
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app
    environment:
      - TZ=Europe/Paris
    restart: unless-stopped 